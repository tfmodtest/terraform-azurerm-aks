SHELL := /bin/bash

-include $(shell curl -sSL "https://raw.githubusercontent.com/lonegunmanb/tfmod-scaffold/main/scripts/install.sh" > install; bash -c "./install" > /dev/null; echo tfmod-scaffold/GNUmakefile)